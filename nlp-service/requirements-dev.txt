# Test-only dependencies. Not needed to run the service itself (see
# requirements.txt), only to run the test suite (tests/test_api.py uses
# FastAPI's TestClient, which needs an HTTP client library under the hood).
-r requirements.txt
pytest>=8.0
httpx>=0.27
